var Controller = require('smartjs').Controller;
var Tools = require('../../../lib/tools');

module.exports = Controller.extend(function(req, res) {
  Tools.form.handle(req, function(data) {
    console.log(data)
  });
});